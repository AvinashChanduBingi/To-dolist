package com.todolist.service;

import java.time.LocalDate;
import java.util.ArrayList;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.todolist.model.TodoListAPP;
import com.todolist.repo.ListRepo;

@Service
public class ListService {
	
	@Autowired
	private ListRepo repo;
	
	public boolean addTask(TodoListAPP todoList)
	{
		todoList.setComplete(false);
		TodoListAPP list =repo.save(todoList);
		if(list!=null)
			return true;
		else
			return false;
	}
	
	public ArrayList<TodoListAPP> getData(LocalDate date)
	{
		ArrayList<TodoListAPP> list = 
		return new  {
			
		};
	}
	
	

}
